language: node_js
node_js:
  - "stable"
cache: npm
script:
  - "npm run build"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $PersonalWeb_Token
  local_dir: build
  on:
    branch: master